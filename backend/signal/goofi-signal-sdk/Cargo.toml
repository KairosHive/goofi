[package]
name = "goofi-signal-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
host = ["goofi-host-sdk/host"]

[dependencies]
goofi-host-sdk = { path = "../../goofi-host-sdk" }
