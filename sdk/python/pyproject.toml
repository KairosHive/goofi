[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "goofi-plugin"
version = "0.1.0"
requires-python = ">=3.11"
description = "Goofi plugin service SDK"
